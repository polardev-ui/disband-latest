import { MarketingHomeGate } from "@/components/marketing/MarketingHomeGate";

export const metadata = {
  title: "Disband — Chat, voice, and communities",
  description: "Disband is a privacy-focused communication platform for desktop and web.",
};

export default function HomeRoute() {
  return <MarketingHomeGate />;
}
