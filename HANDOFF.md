# Disband — session handoff

Paste this whole file to the next assistant, or tell it: **"Read `HANDOFF.md` at
the repo root and continue."**

---

## 1. What this project is

**Disband** is a Discord-style chat platform by Josh Clark (`polar`,
beluga7133@gmail.com). One product, five clients:

| Surface | Stack | Location |
|---|---|---|
| Web + desktop | Next.js 16 (App Router), Tailwind v4, Supabase JS | `src/` |
| Desktop shell | Tauri | `src-tauri/` |
| iOS | Native Swift / SwiftUI, XcodeGen, WebRTC | `ios/` |
| Android | Native Kotlin / Compose | `android/` |
| Backend | Supabase (Postgres + RLS + Realtime + Edge Functions) | `supabase/` |
| CDN | Cloudflare Worker + R2 (`cdn.disband.dev`) | `cloudflare/cdn-worker/` |

Payments are Stripe. Email is Resend. Production Supabase project ref is
`mjqbrcabargylrimlafw`.

**Standing rule from the user's memory: every change bumps semver.** Web =
`package.json`. iOS = `MARKETING_VERSION` / `CURRENT_PROJECT_VERSION` in
`ios/project.yml` (then `xcodegen generate`). Android =
`versionName` / `versionCode` in `android/app/build.gradle.kts`.

**Nothing in this repo is committed.** Everything below is uncommitted working
tree. There are also ~80 older unpushed commits across two divergent histories
— do not try to reconcile that without asking.

---

## 2. The one urgent thing: why the safety work exists

The user seized a space on the platform that was **soliciting child sexual
abuse material, rape, gore and incest**, openly visible. That triggered the
build of "Disband Sentinel". Treat anything touching it as high-stakes and
verify before claiming it works.

### What is LIVE in production right now

* **Migration `supabase/migrations/0090_content_safety.sql`** — applied (in four
  named migrations: `content_safety_schema`, `content_safety_enforcement`,
  `content_safety_bound_text_length`, `content_safety_schedule`). The local file
  has been synced to match what was actually applied.
* **Two views define all user content**, and both the scanner and the
  enforcement path read them, so they cannot drift:
  * `public.safety_media_refs` — avatars, profile banners, space icons/banners,
    group icons, message attachments (channel/DM/group), notes attachments,
    custom emoji, bot avatars, media posts. ~6,830 distinct files.
  * `public.safety_text_refs` — usernames, display names, bios, status notes,
    space names/descriptions, channel names, group names, emoji names, and
    message content everywhere. ~75,000 rows. **Bodies are `left(col, 4000)`**
    on purpose (see gotchas).
* **Tables** (all RLS-on with no policies, execute revoked from
  anon/authenticated — service role only): `banned_media_hashes`,
  `media_assets`, `content_flags`, `moderation_actions`, `moderation_evidence`,
  `safety_runs`.
* **Enforcement functions** (service role only):
  `sentinel_quarantine_media()`, `sentinel_redact_text()`, `sentinel_suspend()`,
  `sentinel_check_key()`.
* **Edge function `content-sentinel`** — deployed with `--no-verify-jwt`.
  Source: `supabase/functions/content-sentinel/{index.ts,rules.ts,rules_test.ts}`.
* **Schedule** — pg_cron job `content-sentinel`, `0 */5 * * *`, active.
  Verified end to end: a real `net.http_post` using the Vault credential
  returned HTTP 200 and completed a run.
* **Text backfill is complete** — all ~75,000 rows scanned. Result: 29 flags,
  of which exactly **1** is `block` severity ("b4 i drop my cp folder bro").
* **Report email works** — verified (`safety_runs.report_sent_at` populated).
  Goes to beluga7133@gmail.com via Resend, metadata only, never imagery.

### Secrets and where they live

* Function secrets (set via `supabase secrets set`): `SENTINEL_KEY`,
  `SAFETY_REPORT_TO`, `SAFETY_REPORT_FROM`, `RESEND_API_KEY`, `CDN_ADMIN_URL`,
  `CDN_ADMIN_SECRET`, `MODERATION_PROVIDER=none`.
* The **cron credential is generated by Postgres and lives only in Vault**
  (`sentinel_key`). The job reads it at execution time; it is not in the job
  definition. `sentinel_check_key()` lets the function verify it without being
  able to read it back. Do not try to print it — the harness blocks credential
  materialisation, and correctly so.
* The manual/operator key was written to `/tmp/sentinel-secrets.env` during the
  session. **`/tmp` may have been cleared** — if you need it, read it from the
  Supabase dashboard function secrets, or just use the Vault path via SQL.

### LEGAL — do not "simplify" these away

* **Never make it delete CSAM on sight.** 18 U.S.C. § 2258A: report to NCMEC,
  then preserve 90 days. The system quarantines to `quarantine/<sha256>` in R2
  (unreachable because `serve()` rejects any key containing `/`) and sets
  `moderation_evidence.preserved_until = now() + 90 days`, with a trigger that
  refuses early deletion.
* **Never put imagery in a report/email/notification.** Ids, hashes, counts only.
* **NCMEC filing is still manual.** The user must register as an ESP at
  <https://report.cybertip.org/espregistration>. Until then someone reviews
  `moderation_evidence where reported_at is null` and files by hand.

### The false-positive lesson — do not regress this

The first dry run over real data produced six `block`-severity text hits.
**Five were false positives, and they were people reporting abuse**: "they have
a child porn channel", "sites on the dark web often advertise child
pornography", "...server that was soliciting child pornography, rape, gore and
incest" (a note about the user's own takedown), plus a crude insult matched
twice. Going live as written would have **banned the reporters**.

Consequences now baked in, keep them:
* **Text never suspends an account.** Every rule has `suspend: false`.
  Suspension requires media evidence (hash match or classifier verdict on an
  actual image).
* A `DISCUSSING` regex demotes `block` → `review` when the text reads as
  reporting/warning/rules/news.
* Those five real strings are regression tests in `rules_test.ts`.
  **Run `deno test supabase/functions/content-sentinel/rules_test.ts` after any
  rules change.** All 4 tests must pass.
* **Always dry-run first** (`{"dryRun":true}`) before enabling anything new.

---

## 3. Outstanding work, in priority order

### (a) Marketing images — the user's most recent explicit ask, NOT STARTED

"Regenerate the marketing images for both iOS and Android, the app got
refreshed." The refresh is the **iOS** redesign (§4); Android still has the old
Compose UI.

* **iOS App Store screenshots.** The iPhone 17 Pro simulator
  (`6042DE8A-CCF5-4232-BE20-1EB220EBCA9D`) has the app installed **and the user
  is signed in** — that session is valuable, don't wipe the device.
  App Store 6.9" needs **1320×2868**; that simulator captures at 1206×2622, so
  either capture on an iPhone 17 Pro Max sim (440×956 @3x = 1320×2868, but it
  won't be signed in) or compose the 1206×2622 captures onto a branded
  1320×2868 canvas with captions. Suggested shots: Home (rail + inbox), a
  server's channel list, a conversation, the voice lobby sheet (opening it does
  **not** join), Friends, You.
  **Do not join a voice channel or start a call to get a screenshot** — it
  rings/announces to real people.
* **Android Play screenshots** are blocked: they need the Android app running
  on an emulator and **signed in**, and you must not enter the user's
  credentials. Ask the user to sign in on an emulator, or to supply captures.
* Already done and still current: Android adaptive launcher icon, the 512×512
  Play icon, and the 1024×500 Play feature graphic — all regenerated from the
  real Disband mark. Generators: `android/tools/make_icons.swift`,
  `android/tools/make_feature_graphic.swift`.

### (b) In-app Disband Aero upgrade — requested earlier, NEVER BUILT

The user asked for: buy Aero inside the iOS app, monthly **and a new yearly
price**, "use Apple Pay… funds should be sent to Stripe", double-click-to-pay.

**Flag this before building:** App Store Guideline 3.1.1 requires **In-App
Purchase** for digital subscriptions. Apple Pay is for physical goods/services
and **cannot** be used to sell Aero in the app; a build doing that gets
rejected. The double-click-side-button confirmation the user is describing is
what StoreKit/IAP shows anyway. Realistic options: (1) StoreKit 2 auto-renewing
subscriptions, reconciled to the existing `subscriptions` table (there are
already `APPLE_PRODUCT_ID_*` env keys and an Apple verify route was deleted at
some point — check history), or (2) keep purchases on the web. Money cannot be
routed to Stripe from IAP. Get the user's decision first.
Stripe has an MCP available in some sessions — check for a yearly price id.

### (c) Close the detection gap

`MODERATION_PROVIDER=none`, so **new imagery is not detected** — only hash
matches. Adapters for Sightengine and Hive are already written in `index.ts`.
Add a key, then backfill media (see §5). Also recommend Cloudflare's free
**CSAM Scanning Tool** on the `cdn.disband.dev` zone.

### (d) Deploy the CDN worker — BLOCKED, needs a human decision

`cloudflare/cdn-worker/src/index.js` now hashes every upload as it streams
(`tee()` + `DigestStream`, so a 500 MB video never enters memory), refuses
denylisted hashes with HTTP 451, registers assets for scanning, and adds
`POST /v1/admin/quarantine`. **Verified locally end to end** (6 MB upload
stored intact, banned hash refused, quarantine requires the secret, quarantined
key unreachable afterwards).

`wrangler deploy` fails:
```
New version of script does not export class 'MediaQuota'
which is depended on by existing Durable Objects.
```
**Production has drifted from source** — the live worker has a `MediaQuota`
Durable Object that exists nowhere in this repo, which means the deployed
script differs in unknown ways. Deploying from the repo would revert those
production-only changes. Do **not** force it. Recover the deployed source from
the Cloudflare dashboard (Workers & Pages → `disband-cdn` → active deployment),
commit it, then re-apply the safety changes on top.
Secrets `SUPABASE_SERVICE_KEY` and `ADMIN_SECRET` are already set on
`disband-cdn`.

### (e) Consider rotating the Supabase service-role key

During the session a `wrangler secret put` ran without `-c`, walked up to the
repo-root `wrangler.jsonc`, and created a stray Worker named `disband`,
briefly placing the service-role key on it. Both secrets were deleted from that
stub (it now has none) and set correctly on `disband-cdn`. The stub Worker
still exists and is harmless. Rotation is the cautious call but has wide blast
radius (`.env.local`, Vercel/Cloudflare env, worker secret, edge functions) —
**ask the user, don't do it unilaterally.**

### (f) Smaller known issues

* **iOS does not count voice minutes.** Web calls `add_voice_minutes`;
  `ios/DisbandiOS/Services/Voice/VoiceSession.swift` does not, so the "Voice
  Veteran" badge can't accrue from mobile.
* **Server voice / group calls on iOS have never been exercised against a real
  web peer.** Protocol interop was derived from the web source and compiles,
  but no live call has been made.
* **Typing dots**: redone on web (`TypingIndicator.tsx` + `globals.css`,
  scale/fade instead of a vertical hop, with a `prefers-reduced-motion`
  variant). Not visually verified in-app (login-gated). **Check whether iOS has
  its own typing indicator** — the user asked for DMs, group chats and servers.
* `CATALYST_PRICE_CENTS = 299` in `src/lib/catalysts.ts` but the live Stripe
  price is 178, so buyers briefly see $2.99 before it corrects. Product
  decision, left alone.
* Duplicate migration number prefixes exist at `0055`, `0057`, `0058`
  (pre-existing).

---

## 4. What changed this session (all uncommitted)

**iOS — version 1.12.0 (49), archived** at
`~/Library/Developer/Xcode/Archives/2026-09-20/DisbandiOS 1.12.0 (49).xcarchive`,
signed for App Store distribution, Organizer opened for the user to upload.

The iOS app was substantially redesigned away from a stock `TabView`:
* New shell: `Views/Shell/` — `AppShell`, `SpacesView` (server rail + panel),
  `ServerRail`, `ChannelPanel`, `InboxPanel`, `FloatingDock`, `ShellChrome`.
* Voice: `Services/Voice/` (`VoiceSession`, `MeshPeer`, `MeshSignal`) and
  `Views/Voice/` (stage, lobby sheet, minimised pill, group-ring banner).
  Full-mesh WebRTC over Supabase broadcast, interoperating with the web's
  `voice:<id>` / `group-call:<id>` protocols and its fixed transceiver "lane"
  layout (audio, camera, screen — order matters).
* Redesigned Friends, Notes, You/settings; added Referrals, custom status with
  expiry, pronouns; `Views/Components/ShellKit.swift` holds shared pieces.
* Fixed: Aero showing as "Disband Basic" **and Aero subscribers reading as
  free** (plan enum didn't know `aero`); group member counts always 0 (which
  also meant group calls rang nobody); markdown leaving `**`/`#` visible and
  italic overwriting bold; banners widening layouts (`BannerImage`); locked
  channels appearing postable; silent send failures; video upload (H.264 MP4
  conversion, streamed from disk); Discovery showing "Join" for joined spaces.
* Removed: `MainTabView`, `ServersTab`, `DirectMessagesTab`, the stub
  `VoiceChannelView`.

**Web — version 2.27.0.** Typing indicator; earlier in the session: server-rail
drop indicator + drag fix, member context menu with kick/ban/timeout reachable
from chat, `TimeoutModal`.

**Backend.** Content safety (§2). Also `supabase/migrations/0089_catalyst_purchase_units.sql`
— fixes a revenue bug where multi-quantity Catalyst orders silently failed
(the unique index couldn't hold N rows per Stripe session); one real customer
order was manually fulfilled.

**Docs.** `docs/CONTENT_SAFETY.md` (operations + legal), a much-expanded
`docs/PLAY_STORE_RELEASE.md`, `docs/runbooks/…` for the catalyst fulfilment.

---

## 5. How to operate the scanner

```bash
URL=https://mjqbrcabargylrimlafw.supabase.co/functions/v1/content-sentinel

# Report-only; changes nothing.
curl -X POST "$URL" -H "Authorization: Bearer $SENTINEL_KEY" \
  -H 'Content-Type: application/json' \
  -d '{"mode":"text","dryRun":true,"full":true,"maxRows":6000}'
```

A **full backfill exceeds one invocation's CPU budget** — drive it in chunks:
each response returns `nextOffset`; feed it back until `null`. ~6,000 text rows
per call is safe. Media: `{"mode":"media","limit":250}`, about 0.5 s per file,
~6,800 files total, so a full media backfill is ~28 runs.

Useful SQL:
```sql
select * from public.content_flags where state='open' order by created_at desc;
select * from public.moderation_actions order by created_at desc limit 50;
select * from public.safety_runs order by started_at desc limit 10;
select id, sha256, category, preserved_until
  from public.moderation_evidence where reported_at is null;
```

---

## 6. Hard-won gotchas — these all cost real time

1. **Supabase `apply_migration` can silently skip a statement.** A
   `drop index` reported success and did nothing. **Always verify with a
   follow-up query** that the object exists / is gone.
2. **The `mcp__supabase__*` tools lack permission on this project.** Use the
   other Supabase MCP server (id beginning `f6a2cde0…`) — it works.
3. **PostgREST caps responses at 1,000 rows.** Asking for 5,000 and treating a
   short page as "done" stopped the first sweep after 1,000 of 75,000 rows.
   Advance by `rows.length`, never by the requested page size.
4. **Edge functions have a hard CPU/memory ceiling** (HTTP 546 /
   `WORKER_RESOURCE_LIMIT`). Fetching rows with megabyte-sized message bodies
   blew it — hence `left(col, 4000)` in the view and a 4,000-char cap in
   `scanText`. Chunk long jobs.
5. **Always pass `-c wrangler.toml` when working in `cloudflare/cdn-worker`.**
   Otherwise wrangler walks up and uses the repo-root `wrangler.jsonc` (the
   Next-on-Workers config, name `disband`) — that is how the stray Worker and
   the misplaced secret happened.
6. **`serve()` in the CDN worker rejects any key containing `/`.** That is what
   makes `quarantine/<sha>` unreachable; don't "fix" it.
7. **The auto-mode classifier blocks production writes from Bash** (Supabase
   REST writes via curl) and blocks printing credentials. Use the Supabase MCP
   for DB writes; for secrets, design so the secret never has to be read (the
   Vault + `sentinel_check_key` pattern).
8. **The app is login-gated and you must never enter the user's credentials.**
   The iPhone 17 Pro simulator is already signed in — use it for verification;
   don't erase it.
9. **Don't take actions that reach real users to get a screenshot** — joining
   voice writes presence, starting a group call rings people and sends pushes.
10. **iOS previews**: Next.js treats `_`-prefixed route folders as private, and
    Xcode/XcodeGen needs `xcodegen generate` after adding files.

---

## 7. How the user expects you to work

* **Verify, then claim.** Build it, run it, screenshot it, query the database.
  Don't report success from having written code.
* **Dry-run anything that removes content or bans accounts**, and read the
  sample before going live.
* Say plainly what you did **not** do or could not verify. The user is fine
  with blockers; they are not fine with silent gaps.
* Match the surrounding code's comment density and voice: comments explain
  **why**, especially where a non-obvious decision prevents a specific bug.
* Bump the version on every change.
* Don't commit unless asked.
