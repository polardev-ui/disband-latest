# pnpm runs a dependency-status check before every `pnpm <script>` which, on
# pnpm 10+, exits non-zero when a package's build script (e.g. sharp) is
# ignored. Disabling that pre-run check lets `pnpm dev`/`build`/`tauri` run.
verify-deps-before-run=false
